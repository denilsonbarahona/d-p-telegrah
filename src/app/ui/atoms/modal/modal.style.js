import styled from "styled-components";

const Dialog = styled.dialog`
  border: none;
  padding: 1.5rem;
  border-radius: 1rem;
  filter: drop-shadow(0 2px 2px var(--light-grey-1));
`;

export default Dialog;
